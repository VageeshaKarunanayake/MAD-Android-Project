<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.optimus.optimus">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainPage"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".OrderlayoutActivity" />
        <activity android:name=".CustomerlayoutActivity" />
        <activity android:name=".OrderTableLayoutActivity" />
        <activity android:name=".InventoryTableLayout" />
        <activity android:name=".InventoryTableLaoutActivivty" />
        <activity android:name=".InventoryLayoutActivity" />
        <activity android:name=".EmployeeLaoutActicvity" />
        <activity android:name=".EmployeeLayoutActivity" />
        <activity android:name=".Customer" />
        <activity android:name=".CustomerTableLaoutActivity" />
        <activity android:name=".EmpolyeeTableLayoutActivity"></activity>
    </application>

</manifest>